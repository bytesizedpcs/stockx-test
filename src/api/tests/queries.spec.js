const assert = require('chai').assert;
const { getShoe, getShoeId, getPost, createShoeTable } = require('../utils/queries');
const { getAllShoes, getShoeById, createShoe } = require('../routes/utils/shoe');

describe('queries.spec.js', () => {
  describe('getAllShoes(req, res)', () => {

  });
  describe('getShoeById(req, res)', () => {

  });
  describe('createShoe(req, res)', () => {

  });
});